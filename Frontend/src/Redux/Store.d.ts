// Store.d.ts

// Import necessary types
import { Store as ReduxStore } from 'redux';

// Define the type of your Redux store
declare const store: ReduxStore;

// Export the store as default
export default store;
